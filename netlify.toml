# Next.js on Netlify — the official runtime plugin handles build output and
# server functions. (Vercel is the primary deploy target; see vercel.json.)
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
